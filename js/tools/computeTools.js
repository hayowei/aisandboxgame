// ============================================
// Compute Tools — 判定/计算类工具
// ============================================
// 当前为空 stub。
// 历史上这里有 get_roll（d20 掷骰判定），但因为设计未完成被屏蔽；
// 已删除 dead code，保留文件 + index.html 引用作为未来挂载点。
// 真要实现时直接在此 IIFE 内 register('get_roll', { ... }) 即可。
// ============================================

(function registerComputeTools() {
  // 暂无注册
})();
